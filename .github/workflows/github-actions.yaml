name: georgia-hello-world-pipeline
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:

    - name: Checkout
      uses: actions/checkout@v2

    - name: lint
      run: |
        ./bin/lint.sh

    - name: test
      run: |
        ./bin/lint.sh






